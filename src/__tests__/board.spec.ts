import Board from '@/components/Board.vue'

describe('board component', () => {
  it('selects character', async () => {
    // controller seleciona ator em 1,1
      // [] ele fica selecionado visualmente
      // [] grid possiveis moves
      // [] toca um som
      // [] tem um vfx



    // ator sabe que fora selecionado e retorna 
    // model de interação sabe que ele esta selecionado

    // controller selecionar espaço em 1,2
    // model move ator em (1,1) para (1,2)

    // controller selecionar ator em 1,1
      // quem selecionou
      // de quem é a vez
      // quem foi selecionado
    // model seleciona ator em 1,1
    // controller selecionar ator em 1,0
    // controller selecionar espaço em 1,2
    // model move ator em (1,0) para (1,2)
  })
})
